import React from "react";
import './styles.css';

const Contact = () => {
    return (
        <section className="contact">
            <h2>Contact Us</h2>
            <button>Contact</button>
        </section>
    );
};

export default Contact;
